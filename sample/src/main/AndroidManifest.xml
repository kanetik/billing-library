<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      `com.android.vending.BILLING` is auto-merged from the play-billing-ktx
      AAR's manifest, so we don't repeat it here. If a consumer is missing it,
      they're depending on play-billing without the library — different problem.
    -->

    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.KanetikBillingSample"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.KanetikBillingSample">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
